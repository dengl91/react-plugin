import React from 'react';
import ReactDOM from 'react-dom/client';
import SettingsPage from './SettingsPage';

const root = ReactDOM.createRoot(document.getElementById('react-plugin-root'));
root.render(
  <React.StrictMode>
    <SettingsPage />
  </React.StrictMode>
);